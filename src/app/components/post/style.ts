import styled from "styled-components";

const PostsContainer = styled.div`
    display: flex;
    flex-direction: column;
`

const PostContent = styled.div`
    display: flex;
    flex-direction: row;
`

const PostList = styled.ul`
    display: flex;
    flex-direction: row;
`

export { PostsContainer, PostContent, PostList }